import { useEffect, useRef, useState } from "react";
import { createFileRoute, useNavigate } from "@tanstack/react-router";
import { Camera, Pencil, Plus, Store, Trash2 } from "lucide-react";
import { toast } from "sonner";
import { api, getSession } from "@/services/api";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export const Route = createFileRoute("/vendor")({ component: VendorDashboardPage });
const empty = { name: "", description: "", price: "", imageUrl: "", available: true };
export function VendorDashboardPage() {
  const navigate = useNavigate(), fileInput = useRef(null); const [vendor, setVendor] = useState(null), [form, setForm] = useState(empty), [editingId, setEditingId] = useState(null), [saving, setSaving] = useState(false), [uploading, setUploading] = useState(false);
  useEffect(() => { if (!getSession()) { navigate({ to: "/login" }); return; } api("/api/vendors/me").then(setVendor).catch((error) => { toast.error(error.message); navigate({ to: "/food" }); }); }, [navigate]);
  const upload = async (file) => { if (!file) return; setUploading(true); try { const body = new FormData(); body.append("file", file); const data = await api("/api/uploads/images", { method: "POST", body }); setForm((current) => ({ ...current, imageUrl: data.imageUrl })); } catch (error) { toast.error(error.message); } finally { setUploading(false); } };
  const create = async (event) => { event.preventDefault(); setSaving(true); try { const payload = { ...form, price: Number(form.price) }; const item = await api(editingId ? `/api/vendors/me/items/${editingId}` : "/api/vendors/me/items", { method: editingId ? "PUT" : "POST", body: JSON.stringify(payload) }); setVendor((current) => ({ ...current, foodItems: editingId ? current.foodItems.map((entry) => entry.foodItemId === editingId ? item : entry) : [item, ...current.foodItems] })); setForm(empty); setEditingId(null); toast.success(editingId ? "Food item updated." : "Food item published."); } catch (error) { toast.error(error.message); } finally { setSaving(false); } };
  const remove = async (item) => { if (!window.confirm(`Remove ${item.name}?`)) return; try { await api(`/api/vendors/me/items/${item.foodItemId}`, { method: "DELETE" }); setVendor((current) => ({ ...current, foodItems: current.foodItems.filter((entry) => entry.foodItemId !== item.foodItemId) })); toast.success("Food item removed."); } catch (error) { toast.error(error.message); } };
  if (!vendor) return <main className="container-shell py-16 text-center text-muted-foreground">Loading vendor dashboard…</main>;
  return <main className="container-shell py-10"><section className="flex flex-col justify-between gap-4 border-b border-border pb-7 sm:flex-row"><div><p className="eyebrow">VENDOR DASHBOARD</p><h1 className="mt-2 font-display text-3xl font-bold">{vendor.name}</h1><p className="mt-2 text-muted-foreground">{vendor.location} · Manage food items and photos.</p></div><Store className="size-10 text-primary"/></section><div className="grid gap-8 py-8 lg:grid-cols-[380px_1fr]"><form onSubmit={create} className="h-fit space-y-4 rounded-xl border border-border bg-card p-5 shadow-soft"><h2 className="font-display text-xl font-bold">{editingId ? "Update food item" : "Post food item"}</h2><Input required placeholder="Food name" value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })}/><textarea placeholder="Description" value={form.description} onChange={(e) => setForm({ ...form, description: e.target.value })} className="min-h-24 w-full rounded-md border border-input bg-background p-3 text-sm"/><Input required min="1" type="number" placeholder="Price (৳)" value={form.price} onChange={(e) => setForm({ ...form, price: e.target.value })}/><input ref={fileInput} type="file" accept="image/jpeg,image/png,image/webp" className="hidden" onChange={(e) => upload(e.target.files?.[0])}/>{form.imageUrl ? <button type="button" className="block w-full" onClick={() => fileInput.current?.click()}><img src={form.imageUrl} alt="Food preview" className="h-36 w-full rounded-lg object-cover"/></button> : <Button type="button" variant="outline" className="w-full" onClick={() => fileInput.current?.click()} disabled={uploading}><Camera/>{uploading ? "Uploading…" : "Upload food photo"}</Button>}<label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.available} onChange={(e) => setForm({ ...form, available: e.target.checked })}/> Available now</label><Button className="w-full" disabled={saving || uploading}><Plus/>{saving ? "Saving…" : editingId ? "Save changes" : "Publish food item"}</Button>{editingId && <Button type="button" variant="outline" className="w-full" onClick={() => { setEditingId(null); setForm(empty); }}>Cancel edit</Button>}</form><section><h2 className="font-display text-xl font-bold">Your food items</h2><div className="mt-4 grid gap-4 sm:grid-cols-2">{vendor.foodItems.map((item) => <article key={item.foodItemId} className="overflow-hidden rounded-xl border border-border bg-card shadow-soft">{item.imageUrl && <img src={item.imageUrl} alt={item.name} className="h-36 w-full object-cover"/>}<div className="p-4"><div className="flex justify-between gap-3"><div><h3 className="font-semibold">{item.name}</h3><p className="mt-1 text-sm text-muted-foreground">৳{item.price} · {item.available ? "Available" : "Hidden"}</p></div><div className="flex gap-2"><Button size="icon" variant="outline" onClick={() => { setEditingId(item.foodItemId); setForm({ name: item.name, description: item.description || "", price: String(item.price), imageUrl: item.imageUrl || "", available: item.available }); }} aria-label={`Edit ${item.name}`}><Pencil/></Button><Button size="icon" variant="destructive" onClick={() => remove(item)} aria-label={`Remove ${item.name}`}><Trash2/></Button></div></div><p className="mt-2 text-sm text-muted-foreground">{item.description}</p></div></article>)}{!vendor.foodItems.length && <p className="rounded-xl border border-dashed border-border py-12 text-center text-muted-foreground sm:col-span-2">No food items posted yet.</p>}</div></section></div></main>;
}
